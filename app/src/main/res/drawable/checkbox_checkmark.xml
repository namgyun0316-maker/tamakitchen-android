<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 체크된 상태 -->
    <item android:state_checked="true">
        <layer-list>
            <!-- 테두리 -->
            <item>
                <shape android:shape="rectangle">
                    <size
                        android:width="20dp"
                        android:height="20dp"/>
                    <solid android:color="@android:color/white"/>
                    <stroke
                        android:width="2dp"
                        android:color="#4A90A4"/>
                    <corners android:radius="4dp"/>
                </shape>
            </item>

            <!-- ✔ 체크 표시 -->
            <item
                android:left="4dp"
                android:top="2dp">
                <vector
                    android:width="12dp"
                    android:height="12dp"
                    android:viewportWidth="24"
                    android:viewportHeight="24">
                    <path
                        android:fillColor="#4A90A4"
                        android:pathData="M20,6L9,17L4,12l1.5,-1.5L9,14L18.5,4.5z"/>
                </vector>
            </item>
        </layer-list>
    </item>

    <!-- 체크 안 된 상태 -->
    <item>
        <shape android:shape="rectangle">
            <size
                android:width="20dp"
                android:height="20dp"/>
            <solid android:color="@android:color/white"/>
            <stroke
                android:width="2dp"
                android:color="#999999"/>
            <corners android:radius="4dp"/>
        </shape>
    </item>

</selector>
