<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid android:color="#FFFFFF"/>

    <!-- 둥근 모서리 -->
    <corners android:radius="18dp"/>

    <!-- 연한 테두리 -->
    <stroke
        android:width="1dp"
        android:color="#E6EAF0"/>

    <!-- 내부 여백 -->
    <padding
        android:left="14dp"
        android:top="12dp"
        android:right="14dp"
        android:bottom="12dp"/>
</shape>
